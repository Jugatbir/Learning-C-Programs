#include<stdio.h>
int main() {
    int a;
    a = 10;

    // The value of variable is placed in the place of %d
    // in the printf function
    // %d is a format specifier for integers
    
    printf("The output of the program is %d", a);
    return 0;
}